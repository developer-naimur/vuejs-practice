/* =====================================================
   ROUTES (GLOBAL)
===================================================== */
export const $routes = {
  index   : '/account/fund-transfer',
  create  : '/account/fund-transfer/create',
  trash   : '/account/fund-transfer/trashed',
}

/* =====================================================
   PAGE TEXT / LABELS
===================================================== */
export const $labels = {
  plural_name   : 'Fund Transfers',
  singular_name : 'Fund Transfer',
}
