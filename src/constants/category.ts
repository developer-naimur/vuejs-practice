/* =====================================================
   ROUTES (GLOBAL)
===================================================== */
export const $routes = {
  index   : '/product/category',
  create  : '/product/category/create',
  trash   : '/product/category/trashed',
}

/* =====================================================
   PAGE TEXT / LABELS
===================================================== */
export const $labels = {
  plural_name   : 'Categories',
  singular_name : 'Category',
}
