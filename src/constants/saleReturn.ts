/* =====================================================
   ROUTES (GLOBAL)
===================================================== */
export const $routes = {
  index   : '/sale-return',
  create  : '/sale',
  trash   : '/sale-return/trashed',
}

/* =====================================================
   PAGE TEXT / LABELS
===================================================== */
export const $labels = {
  plural_name   : 'Sale Returns',
  singular_name : 'Sale Return',
}
